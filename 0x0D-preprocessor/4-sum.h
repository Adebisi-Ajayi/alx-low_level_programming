#include <stdio.h>
/**
 * main - print function
 *
 * Return: return main
 */
int main(void)
{
	int x = 32;
	int y = 8;
	int res = SUM(x, y);

	printf("The sum of %d and %d is %d\n", x, y, res);
	return (0);
}
