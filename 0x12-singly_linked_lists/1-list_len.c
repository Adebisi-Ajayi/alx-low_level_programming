#include "lists.h"
#include <stdio.h>
/**
 * list_len - lengthof list
 * @h: string to print
 * Return: strings
 */
size_t list_len(const list_t *h)
{
	size_t ade = 0;

	while (h != NULL)
	{
		add++;
		h = h->next;
	}
	return (ade);
}
